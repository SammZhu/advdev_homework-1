#!groovy

// Jenkinsfile for MLBParks
// Create a slave 
  podTemplate(
  label: "mlbparks-build",
  inheritFrom: "maven",
  cloud: "openshift",
  containers: [
    containerTemplate(
      name: "jnlp",
      image: "docker-registry.default.svc:5000/cpd-jenkins/maven-slave-pod",
      resourceRequestMemory: "1Gi",
      resourceLimitMemory: "2Gi"
    )
  ]
) {
  node('mlbparks-build') {

    // Define maven command to use Nexus
    def mvnCmd = "mvn -s ./advdev_homework/nexus_settings.xml"

    // Extract version and other properties from the pom.xml
    def groupId    = getGroupIdFromPom("./advdev_homework/pom.xml")
    def artifactId = getArtifactIdFromPom("./advdev_homework/pom.xml")
    def version    = getVersionFromPom("./advdev_homework/pom.xml")

    // Checkout Source Code
    stage('Checkout Source') {
      git url: 'https://github.com/carl-durrant/advdev_homework.git'
    }    
    // Using Maven build the war file
    stage('Build war') {
      echo "Building version"
    }
  }
}
  
